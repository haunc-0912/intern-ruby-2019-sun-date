var conversation = $('#conversations-list').find('[data-conversation-id=<%= @conversation.id %>]');
//hide all class conversation
conversation.parent().parent().children().addClass('hide_conversations');
